using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Pun;

public class Zenga : MonoBehaviourPun
{
    // Start is called before the first frame update
    void Start()
    {
        //GameManager.instance.zengaList.Add(photonView);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
   
}
